# Environment and secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
app/.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage
*.egg-info/
dist/
build/

# uv (keep uv.lock committed)
# uv.lock — committed intentionally

# Terraform (ADR 0001: bootstrap uses local state)
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Bootstrap local state (explicit)
infra/bootstrap/terraform.tfstate
infra/bootstrap/terraform.tfstate.*

# Main stack remote backend config (operator-specific; use backend.hcl.example)
infra/envs/prod/backend.hcl

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Docker
*.log

# Build artifacts
*.local
